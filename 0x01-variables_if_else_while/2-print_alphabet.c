#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char clara = 'a';

	for (clara = 'a'; clara <= 'z'; clara++)
		putchar(clara);

	putchar('\n');

	return (0);
}
