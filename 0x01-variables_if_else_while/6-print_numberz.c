#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int akan = '0';

	for (akan = '0'; akan <= '9'; akan++)
		putchar(num);

	putchar('\n');
	return (0);
}
